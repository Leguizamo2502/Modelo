export interface form{
    id:number;
    name:string;
    description:string;
    active:boolean;
    is_deleted:boolean;
}

export interface formCreate{
    // id:number;
    name:string;
    description:string;
    active:boolean;
}